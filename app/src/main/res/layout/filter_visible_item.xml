<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <data>
        <variable
            name="titlable"
            type="com.rudearts.soundapp.model.filter.Titlable"/>
    </data>

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <TextView
            android:id="@+id/title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            style="@style/FilterVisibleItemTitle"
            android:text="@{titlable.getTitleId}"
            tools:text="TextView"/>
    </RelativeLayout>
</layout>